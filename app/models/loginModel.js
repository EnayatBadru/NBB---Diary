// loginModel.js
class Login {
    constructor(email, password) {
      this.email = email;
      this.password = password;
    }
  }
  
  // Disponibiliza a classe Login globalmente para ser usada em outros scripts
  window.Login = Login;
  